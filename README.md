# Python-Learning
# Python For Loops

A 'for' loop allows repeating an action multiple times.
Example:
for i in range(5):
    print(i)

Use cases:
- Iterating over numbers
- Iterating over lists
- Repeating tasks automatically
